apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

ext.projectArtifactId   = "dbassist-annotations"

apply from: rootProject.file('gradle/bintrayUpload.gradle')